<%= form_for ([@trail, @comment]) do |f| %>

    <%= f.label :reply, "Comment:"%>
    <br>
    <%= f.text_area :reply %>
    <br>
    <%= f.hidden_field :trail_id, value: @trail.id%>
    <%= f.submit %>
<%end %> 