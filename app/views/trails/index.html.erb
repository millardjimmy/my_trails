<h1> Welcome to Every Trail! </h1>
<h3> Click on a trail to explore! </h3>
<h4> Know a trail we dont? <%= link_to "Create a trail!", new_trail_path(@trail) %> </h4>
<% @trails.each do |t| %>
    <%= t.name %>
    <%= t.city %>
    <%= t.length %> 
    <%= t.skill_length %>
    <%= t.description %>
<%end%>
